
#include "quicksort.h"
#include <stdlib.h>
#include <stdio.h>

int* quicksort(int* arr, int tam) {

    int meio = tam / 2;
    int pivot = arr[meio];
    int aux = 0;

    if (tam == 0) return NULL;
    if (tam == 1) return arr;




}